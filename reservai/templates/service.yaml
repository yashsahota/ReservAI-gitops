apiVersion: v1
kind: Service
metadata:
  name: reservai
spec:
  type: {{ .Values.service.type }}
  loadBalancerIP: {{ .Values.service.loadBalancerIP }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: reservai
